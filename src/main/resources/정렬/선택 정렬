
# 선택 정렬
    1. 간단한 정렬 알고리즘
    2. 목록을 총 n-1번 훑으면서 다음 과정을 반복
        - 첫 요소 0부터 훑으면서 최솟값을 찾아 요소 0과 교환
        - 첫 요소 1부터 훑으면서 최솟값을 찾아 요소 1과 교환
        - 첫 요소 2부터 훑으면서 최솟값을 찾아 요소 2과 교환
        ...
        - n-1까지 반복
    3. 최솟값을 찾아 선택한다고 해서 선택 정렬
    4. 안정성이 유지되지 않는다.
    5. 예시
        5 3 1 3@ 4 6 2 가 있다고 하고 0번째 인덱스 부터 낮은 순서대로 쭉 정렬한다고 했을 때
        1회차 : 버블 정렬은 index0 index1을 비교 후 교환/유지 하지만 선택 정렬은 쭉 훑으면서 제일 작은 값을 찾아 index0에 넣는다.
        2회차 : 1 다음 가장 작은 값이 2이기 때문에 3과 2가 교환된다. 이 때 여기에서 3과 3@의 순서가 바뀜 ( 3이 앞이어야 하는데 2의 자리인 뒤로감)
        --> 안정성이 깨진다.
        쭉 반복
        1 2 3@ 3 4 5 6 이 정렬된다.

    6. 시간 공간 복잡도는 버블과 비슷하다.


